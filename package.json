{
  "name": "flip-recruitment-task",
  "scripts": {
    "lint:s0DataProvider": "eslint 'services/s0DataProvider/**/*.ts'",
    "lint:all": "eslint '**/*.ts'",
    "format:s0DataProvider": "eslint 'services/s0DataProvider/**/*.ts' --fix",
    "format:all": "eslint '**/*.ts' --fix",
    "jest:s0DataProvider": "jest services/s0DataProvider",
    "jest:all": "jest services",
    "jest:all:debug": "node --inspect-brk=0.0.0.0 node_modules/.bin/jest --runInBand services"
  },
  "jest": {
    "preset": "ts-jest",
    "setupFilesAfterEnv": [
      "./shared/test/jest.setup.ts"
    ]
  },
  "dependencies": {
    "eslint-config-airbnb-base-ts": "^14.1.2",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1"
  }
}
