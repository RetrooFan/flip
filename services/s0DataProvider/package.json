{
  "name": "s0-data-provider",
  "main": "dist/services/s0DataProvider/src/main.js",
  "scripts": {
    "build:clean": "rimraf dist",
    "prebuild": "npm run build:clean",
    "build": "tsc",
    "preprod": "npm run build",
    "prod": "node .",
    "dev": "nodemon"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.11",
    "@nestjs/config": "^2.2.0",
    "@nestjs/core": "^9.0.11",
    "@nestjs/mongoose": "^9.2.0",
    "@nestjs/platform-express": "^9.0.11",
    "@types/node": "^18.7.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1"
  }
}
